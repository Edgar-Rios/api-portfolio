const repository = require('../repositories/home');

module.exports = {

    "info": async (req) => {

        try {
            
            // ALGO......

        } catch (error) {
            
        }

    }

}